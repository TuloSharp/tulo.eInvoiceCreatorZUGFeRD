<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:tulo.FontSizePicker">
    <Style TargetType="{x:Type local:FontSizePicker}">
        <Setter Property="Focusable" Value="False"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:FontSizePicker}">
                    <StackPanel Orientation="Horizontal"
                                VerticalAlignment="Center"
                                HorizontalAlignment="Left">

                        <!-- A small -->
                        <Button Style="{x:Null}"
                                Padding="0"
                                Margin="0"
                                Width="22" Height="22"
                                Background="Transparent"
                                BorderThickness="0"
                                Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"
                                Command="{x:Static Slider.DecreaseLarge}"
                                CommandTarget="{Binding ElementName=PART_Slider}">
                            <TextBlock Text="A"
                                       FontSize="{TemplateBinding SmallALabelSize}"
                                       FontWeight="SemiBold"
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Center"/>
                        </Button>

                        <TextBlock Margin="6,0"
                                   VerticalAlignment="Center"
                                   Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"
                                   Text="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=Value}"
                                   Opacity="0.75"/>

                        <!-- A big -->
                        <Button Style="{x:Null}"
                                Padding="0"
                                Margin="0"
                                Width="22" Height="22"
                                Background="Transparent"
                                BorderThickness="0"
                                Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"
                                Command="{x:Static Slider.IncreaseLarge}"
                                CommandTarget="{Binding ElementName=PART_Slider}">
                            <TextBlock Text="A"
                                       FontSize="{TemplateBinding LargeALabelSize}"
                                       FontWeight="SemiBold"
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Center"/>
                        </Button>

                        <!-- Invisible slider: only for commands, not operable -->
                        <Slider x:Name="PART_Slider"
                                Visibility="Collapsed"
                                Minimum="{TemplateBinding Minimum}"
                                Maximum="{TemplateBinding Maximum}"
                                TickFrequency="{TemplateBinding Step}"
                                IsSnapToTickEnabled="True"
                                Value="{Binding RelativeSource={RelativeSource TemplatedParent},
                                                Path=Value,
                                                Mode=TwoWay,
                                                UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
